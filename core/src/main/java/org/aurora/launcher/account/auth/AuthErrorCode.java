package org.aurora.launcher.account.auth;

public enum AuthErrorCode {
    NETWORK_ERROR,
    INVALID_CREDENTIALS,
    USER_CANCELLED,
    TOKEN_EXPIRED,
    SERVER_ERROR,
    NO_MINECRAFT_PROFILE,
    RATE_LIMITED
}